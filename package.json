{
  "name": "mvviewer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:app:web": "npm run build --workspace=@mvviewer/web",
    "local:preview": "npm run build:app:web && npx serve apps/web/dist",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "latest",
    "happy-dom": "^20.11.0",
    "typescript": "latest",
    "vitest": "latest"
  }
}
